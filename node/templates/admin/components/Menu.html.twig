<nav class="sidebar">
    {% for item in items %}
        <div class="nav-item{{ this.isActive(item.route) ? ' active' : '' }}">
            <div class="nav-icon"></div>
            <a href="{{ path(item.route) }}">{{ item.label }}</a>
        </div>
    {% endfor %}
</nav>
